# Copy this file to .env and fill in your values. .env is gitignored.
#
#   cp .env.example .env
#
# OpenRouter API key, used by the Layer 1 LLM driver (axprobe run --model ...).
# Get one at https://openrouter.ai/keys
OPENROUTER_API_KEY=sk-or-...
